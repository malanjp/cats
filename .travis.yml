language: python

python:
  - 2.7

install:
  - "pip install -r requirements.txt --use-mirrors"

script: 
  - py.test

notifications:
  emails:
    - mshibata@i.softbank.jp
  on_success: change
  on_failure: always


