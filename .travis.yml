language: python
python:
  - "2.7"
#  - "3.3"
before_script:
  - pip install -r requirements.txt --use-mirrors
script:
  - py.test 

notifications:
  emails:
    - mshibata@i.softbank.jp
  on_success: change
  on_failure: always

